// Not used: LooperAudio registers via the Swift CAPBridgedPlugin conformance
// (LooperAudio.swift) plus its entry in the iOS packageClassList (added by
// native/scripts/patch-ios-config.mjs during `npm run sync`). This file is
// intentionally empty and can be removed from the Xcode target.
#import <Foundation/Foundation.h>
