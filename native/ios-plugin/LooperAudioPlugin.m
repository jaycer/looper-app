// Capacitor 6 registers plugins via the Swift `CAPBridgedPlugin` protocol
// (see LooperAudio.swift), so the old Objective-C `CAP_PLUGIN` macro is no
// longer used. This file is intentionally left empty; it can be removed from
// the Xcode target entirely.
#import <Foundation/Foundation.h>
